name: Translate Documentation

on:
  push:
    branches: [main]
    paths:
      - 'src/content/docs/index.mdx'
      - 'src/content/docs/getting-started/**'
      - 'src/content/docs/concepts/**'
      - 'src/content/docs/guides/**'
      - 'src/content/docs/reference/**'
      - 'src/content/docs/adrs/**'

permissions:
  contents: write
  models: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  translate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/cache@v4
        with:
          path: .genaiscript/cache/**
          key: continuous-translation-${{ github.run_id }}
          restore-keys: |
            continuous-translation-

      - uses: pelikhan/action-continuous-translation@v0
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          starlight_dir: .
          lang: es,ru,fr,ja
          source: en
          files: "src/content/docs/**/*.mdx;src/content/docs/**/*.md"
          debug: true

      - uses: stefanzweifel/git-auto-commit-action@v5
        with:
          file_pattern: 'src/content/docs/**'
          commit_message: '[translation] auto-translate docs'
          commit_user_name: genaiscript
